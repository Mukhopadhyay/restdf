summary: "Index route, gives brief intro about the API"
description: "Returns each endpoint's type & description"
tags:
  - 'Docs'
consumes:
  - 'application/json'
produces:
  - 'application/json'
responses:
  200:
    description: "Success"
    schema:
      type: "array"
      example: [{'name': '/','type': ['GET'], 'description': ''}]
