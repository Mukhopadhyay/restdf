summary: "Returns the datatypes of all columns"
description: "This endpoint returns the response from <code>df.dtypes</code> & returns the result."
tags:
  - 'Metadata'
consumes:
  - 'application/json'
produces:
  - 'application/json'
responses:
  200:
    description: "Success"
    schema:
      type: "object"
      example: {'dtypes': {"Age": "float64", "Cabin": "object", "Embarked": "object"}}


